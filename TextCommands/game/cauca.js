const Discord = require('discord.js')

module.exports = {
    name: "cauca",
    aliases: ["cauca", "fish"],
    description: "Lệnh cho phép member cauca trong server",
    /**
     *
     * @param {Discord.Client} client
     * @param {Discord.Message} message
     * @param {*} args
     * @param {*} userData
     */
    run: async (client, message, args, userData) => {
    

    }
}